import TeamManager from "@/components/Main Admin/management/team manager/TeamManager";
import React from "react";

type Props = {};

const teammanager = (props: Props) => {
  return (
    <div>
      <TeamManager />
    </div>
  );
};

export default teammanager;
