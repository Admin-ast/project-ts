import ChatSupport from "@/components/Main Admin/management/chat support/ChatSupport";
import React from "react";

type Props = {};

const chatsupport = (props: Props) => {
  return (
    <div>
      <ChatSupport />
    </div>
  );
};

export default chatsupport;
