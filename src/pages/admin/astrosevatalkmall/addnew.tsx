import AddNew from "@/components/Main Admin/astrosevatalk mall/Add New/AddNew";
import React from "react";

type Props = {};

const addnew = (props: Props) => {
  return (
    <div>
      <AddNew />
    </div>
  );
};

export default addnew;
