import Users from "@/components/Main Admin/Users/Users";
import React from "react";

type Props = {};

const user = (props: Props) => {
  return (
    <div>
      <Users />
    </div>
  );
};

export default user;
