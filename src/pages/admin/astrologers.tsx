import Astrologers from "@/components/Main Admin/astrologers/Astrologers";
import React from "react";

type Props = {};

const astrologers = (props: Props) => {
  return (
    <div>
      <Astrologers />
    </div>
  );
};

export default astrologers;
