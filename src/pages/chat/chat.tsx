import ChatForm from "@/components/chat/ChatForm";
import React from "react";

type Props = {};

const chat = (props: Props) => {
  return (
    <div className="">
      <ChatForm />
    </div>
  );
};

export default chat;
