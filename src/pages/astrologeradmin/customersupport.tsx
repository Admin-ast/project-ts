import CustomerSupport from "@/components/admin/support chat/customer support/CustomerSupport";
import React from "react";

type Props = {};

const customersupport = (props: Props) => {
  return (
    <div>
      <CustomerSupport />
    </div>
  );
};

export default customersupport;
